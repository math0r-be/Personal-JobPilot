# Base de données locale SQLite (chemin relatif au projet)
# Le fichier sera créé automatiquement lors du premier `npm run db:push`
DATABASE_URL="file:./local.db"

# Aucune autre variable d'environnement n'est requise.
# La configuration de l'IA et du SMTP se fait directement dans l'interface Réglages de l'app.
